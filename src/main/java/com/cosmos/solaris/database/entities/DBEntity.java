package com.cosmos.solaris.database.entities;

//This exists, so I can easier manipulate all entities without worrying about their type
public interface DBEntity {
	public boolean equals(Object o);
}
